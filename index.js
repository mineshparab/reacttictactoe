// StrictMode is a tool for highlighting potential problems in an application. Like Fragment , StrictMode does not render any visible UI. 
//It activates additional checks and warnings for its descendants. Note: Strict mode checks are run in development mode only; 
//they do not impact the production build.

import { StrictMode } from 'react';

/*
createRoot lets you create a root to display React components inside a browser DOM node.
*/
import { createRoot } from 'react-dom/client';
import './style.css' ;
import App from './App';
